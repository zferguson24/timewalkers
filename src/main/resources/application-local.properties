# Local profile — activates full DEBUG tracing for the application package.
# DEBUG logs are suppressed in all other environments (see application.properties).
logging.level.com.wow.timewalkers=DEBUG
